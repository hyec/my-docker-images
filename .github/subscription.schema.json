{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Image subscription",
  "description": "Rules for tracking a public Docker Hub or GHCR base image.",
  "type": "object",
  "additionalProperties": false,
  "required": ["enabled", "upstream", "tag_regex", "platforms"],
  "properties": {
    "enabled": {
      "type": "boolean",
      "description": "Whether the scheduled workflow may publish this image."
    },
    "upstream": {
      "type": "string",
      "pattern": "^(docker\\.io|ghcr\\.io)/[a-z0-9_./-]+$",
      "description": "Fully qualified upstream repository, without a tag or digest.",
      "examples": ["docker.io/library/alpine", "ghcr.io/astral-sh/uv"]
    },
    "tag_regex": {
      "type": "string",
      "minLength": 1,
      "description": "Python regular expression matched against the whole upstream tag."
    },
    "sort": {
      "type": "string",
      "enum": ["semver", "lexicographic"],
      "default": "semver"
    },
    "platforms": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "pattern": "^linux/[a-z0-9]+(/[a-z0-9]+)?$"
      },
      "description": "Desired Linux platforms in preference order."
    }
  }
}
